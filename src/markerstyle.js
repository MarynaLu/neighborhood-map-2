import pin from './pin.svg';

const markerStyle = {
  position: 'absolute',
  width: 30,
  height: 30,
  borderRadius: '50%',
  textAlign: 'center',
  fontSize: 16,
  padding: 0,
  fontWeight: 'bold',
    backgroundSize: 'cover',
  background: `url(${pin})`,   
};